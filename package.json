{
  "devDependencies": {
    "turbo": "^1.10.7"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/src/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
