import { Stack } from "expo-router";
import React from "react";

type Props = {};

const _layout = (props: Props) => {
  return <Stack screenOptions={{ title: "New Transaction" }} />;
};

export default _layout;
